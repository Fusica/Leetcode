#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


class Solution {
public:
    static vector<int> printNumbers(int n) {
        // 空
    }
};

int main() {
    int n;
    cin >> n;
    vector<int> ans;
    ans = Solution::printNumbers(n);

    return 0;
}
